export type Coordinates = {
  x: number,
  y: number
}

export type ColorTypes = {
  car: string;
  carContent: string;
  connector: string;
  grid: string;
  locomotive: string;
  locomotiveAccent: string;
  locomotiveWindow: string;
  track: string;
}
